# Lead Intelligence Demo

Sanitized static demo of the lead scraping pipeline output. **No real business data.**

Open `index.html` via a local server:

```powershell
cd demo
python -m http.server 5180
```

Then visit http://localhost:5180
