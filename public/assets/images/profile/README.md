# Profile Image

Drop a professional headshot here named `photo.jpg`.

Recommended: square crop, 400×400px minimum, well-lit, professional but human.

If no image is uploaded, the about section renders without a photo.
Optionally add an `<img>` in the About section pointing to ./assets/images/profile/photo.jpg
with an onerror handler to hide if missing.
