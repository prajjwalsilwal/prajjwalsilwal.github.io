# Diagnostic Dashboard Demo

Sanitized static demo of the multi-client IT diagnostic dashboard. **No real endpoint data.**

Open `index.html` via a local server:

```powershell
cd demo
python -m http.server 5181
```

Then visit http://localhost:5181
